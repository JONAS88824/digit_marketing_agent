# ===== 敏感文件：绝不提交 =====
# API key 与真实凭证
.env
# 服务账号密钥。刻意用通配符全拦：密钥文件可以叫任何名字，
# 宁可挡住一个无关的 json，也不能漏放一个密钥。
# 确实需要提交某个 json 时，用 git add -f 单独放行。
*.json

# ===== 构建与运行产物 =====
__pycache__/
*.pyc
.adk/
generated/

# Web UI 持久化数据（会话 SQLite + 产物图片）
web/.data/
